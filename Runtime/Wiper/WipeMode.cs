namespace RiseOn.Wipe2D {
    public enum WipeMode {
        /// <summary>Wherever the wiper passes, the sprite disappears.</summary>
        Erase

       ,

        /// <summary>Wherever the wiper passes, the sprite returns to its original look.</summary>
        Reveal
    }
}